def numberOfSteps(self, num: int) -> int:
        stepscount= 0;
        while num > 0 :
            if num%2 == 0 :
                num = num/2
            else:
                num =  num- 1

            stepscount= stepscount +1

        return stepscount
